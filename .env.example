# Generate with: openssl rand -base64 48
JWT_SECRET=generate-a-strong-random-secret-min-64-chars
# Restrict to your frontend origin (never use *)
CORS_ORIGIN=https://your-frontend-domain.com
KAFKA_BROKERS=localhost:9092
SSO_DB_USER=sso_user
# Generate with: openssl rand -base64 24
SSO_DB_PASSWORD=change-me-use-strong-random-password
SSO_DB_NAME=sso_db
BANKING_DB_USER=banking_user
# Generate with: openssl rand -base64 24
BANKING_DB_PASSWORD=change-me-use-strong-random-password
BANKING_DB_NAME=banking_db
# Leave empty to disable webhooks, or set a trusted HTTPS URL
WEBHOOK_URL=
